"----------------------------------------------------------------------------
"
" caw.vim
"
" see: https://github.com/tyru/caw.vim
" echo "load caw.vim configration file."

" デフォルトのキーマッピングを無効化
let g:caw_no_default_keymappings = 1

nmap <Leader>/    <Plug>(caw:hatpos:toggle)
vmap <Leader>/    <Plug>(caw:hatpos:toggle)

